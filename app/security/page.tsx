import { Check, LockKeyhole } from 'lucide-react'
import { CtaBand, PageIntro, SectionLabel, SiteShell } from '@/components/site-shell'

export default function SecurityPage() { return <SiteShell><main><PageIntro kicker="Trust by design" title={<>Your data is yours.<br /><em>Always.</em></>} body="Intelligence only creates value when it is built on trust. Lunacore is designed to make your business clearer without compromising the information that makes it yours." /><section className="section shell security-grid"><div className="security-statement"><LockKeyhole size={28} /><h2>Private by<br /><em>default.</em></h2><p>We treat every connection, metric, and conversation as confidential. Your information is never sold, shared, or used to train a model for anyone else.</p></div><div className="security-points">{[['Your data, your control','You decide what connects, who sees it, and when access ends.'],['Built for responsible AI','Human judgment stays in the loop. Lunacore highlights patterns; you make the call.'],['Quietly secure','Encryption in transit and at rest, least-privilege access, and regular review.'],['Transparent always','No black boxes. Every signal comes with context you can understand and challenge.']].map(([title, body]) => <div className="security-point" key={title}><span><Check size={16} /></span><div><h3>{title}</h3><p>{body}</p></div></div>)}</div></section><section className="dark-section"><div className="shell security-quote"><SectionLabel>Our principle</SectionLabel><blockquote>“The best intelligence does not replace your judgment. It gives your judgment more to work with.”</blockquote><span>— Lunacore team</span></div></section><CtaBand title="Bring us the hard questions." /></main></SiteShell> }
