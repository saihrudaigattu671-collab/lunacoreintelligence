import { Check } from 'lucide-react'
import { ArrowLink, CtaBand, PageIntro, SectionLabel, SiteShell } from '@/components/site-shell'

const plans = [{ name: 'Essential', price: '₹25,000', description: 'For focused teams building a clearer operating rhythm.', items: ['Core signal dashboard', 'Weekly intelligence brief', 'Up to 3 data sources'] }, { name: 'Advantage', price: '₹45,000', description: 'For growing teams ready to make intelligence a habit.', items: ['Everything in Essential', 'Daily decision signals', 'Executive reporting layer', 'Up to 8 data sources'], featured: true }, { name: 'Custom', price: 'Let’s talk', description: 'For complex businesses with a bigger picture to connect.', items: ['Everything in Advantage', 'Custom intelligence model', 'Dedicated strategic support'] }]
export default function PricingPage() { return <SiteShell><main><PageIntro kicker="Simple, outcome-based" title={<>A clearer return<br />on your attention.</>} body="Start with the level of intelligence your business needs today. Grow into the system as your ambition grows." /><section className="section shell"><div className="pricing-grid">{plans.map((plan) => <article className={`price-card ${plan.featured ? 'featured' : ''}`} key={plan.name}>{plan.featured && <span className="popular">Most chosen</span>}<SectionLabel>{plan.name}</SectionLabel><h2>{plan.price}{plan.price !== 'Let’s talk' && <small>/ month</small>}</h2><p>{plan.description}</p><ul>{plan.items.map((item) => <li key={item}><Check size={15} />{item}</li>)}</ul><ArrowLink href="/contact" dark={plan.featured}>{plan.name === 'Custom' ? 'Start a conversation' : 'Choose this level'}</ArrowLink></article>)}</div><p className="pricing-note">All plans include a one-time setup fee of ₹10,000 + 18% GST. Every engagement starts with a focused onboarding session to map your business and define your highest-value signals.</p></section><CtaBand title="The cost of not knowing is usually higher." /></main></SiteShell> }
